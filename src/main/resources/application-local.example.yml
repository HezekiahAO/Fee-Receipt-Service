spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/fee_receipt_db?sslmode=disable
    username: postgres
    password: CHANGE_ME

app:
  webhook:
    secret: temporary-test-secret-123